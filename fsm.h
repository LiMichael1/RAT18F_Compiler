#pragma once
#include <iostream>
#include <vector>
#include <string>


int FSM_REAL_INT(char*);

int FSM_ID(char*);

int isKeyword(char*);

int isOperator(char*);

int isSeparator(char*);

void Unknown();