#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <ctype.h>

int FSM(char*);

int isKeyword(char*);

int isOperator(char*);

int isSeparator(char*);

int Unknown(char);